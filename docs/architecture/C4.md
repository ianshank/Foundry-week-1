# Architecture — C4 view

Three levels, because the fourth (code) is the source and would go stale.
Everything below describes **week 1 only**: local, stdio, zero Azure spend.
The week-2 shape is at the bottom, drawn separately, because the difference
between the two is the whole point of the spike's verdict.

---

## C1 — System context

Who touches the spike, and what stays outside it.

```mermaid
graph TB
    dev["Engineer<br/><i>runs the 5 sessions</i>"]

    subgraph spike["Foundry week-1 spike <b>(this repo)</b>"]
        bench["Bake-off bench<br/>+ read-only MCP tools<br/>+ evidence"]
    end

    toolkit["Foundry Toolkit<br/><i>VS Code extension</i><br/>Playground · Agent Builder"]
    gh["GitHub Models<br/><i>free tier</i>"]
    ollama["Ollama<br/><i>local GPU box</i>"]
    onnx["ONNX / Foundry Local<br/><i>quantized, may be dropped</i>"]

    planlint[("planlint<br/><i>CLI, exit 0/1/2</i>")]
    harness[("eval-harness<br/><i>json_file sink</i>")]
    agents[("Agents repo<br/><i>OpenSpec specs</i>")]

    dev --> spike
    dev --> toolkit
    toolkit --> gh
    toolkit --> ollama
    toolkit --> onnx
    toolkit -->|"stdio MCP"| spike

    spike -->|"subprocess, read-only"| planlint
    spike -->|"reads sink files"| harness
    planlint -->|"lints"| agents

    azure["Azure / Foundry Agent Service"]
    spike -.->|"<b>NOT this week</b>"| azure

    classDef out fill:#eee,stroke:#999,stroke-dasharray:4 3,color:#333
    classDef ext fill:#f5f5f5,stroke:#777,color:#222
    class azure out
    class planlint,harness,agents,gh,ollama,onnx ext
```

**Read the arrows for direction of trust.** Everything flows *out* of the
spike: it reads `planlint` and the harness sink and never writes to either. No
commits reach `Mango_Code_Agent-Harness`, `Agents` or `planlint` this week, and
`mcp_server/tests/test_seam_is_closed.py` fails if the wrapper starts importing
what it should be calling.

---

## C2 — Containers

The runnable pieces and what each one is for.

```mermaid
graph LR
    subgraph vs["VS Code"]
        pg["Model Playground<br/><i>step 2 · Compare</i>"]
        ab["Agent Builder<br/><i>step 4 · prompt agent</i>"]
    end

    subgraph repo["This repository"]
        server["<b>mcp_server/</b><br/>stdio MCP server<br/><i>Python, 2 tools</i>"]
        probe_facade["<b>scripts/verifier_probe.py</b><br/>facade entry point<br/><i>backwards compatible</i>"]
        subgraph probe_pkg["<b>scripts/probe/</b><br/>modular probe package"]
            probe_cli["cli.py"]
            probe_runner["runner.py"]
            probe_screen["screen.py"]
            probe_client["client.py"]
            probe_config["config.py"]
        end
        baseline["<b>scripts/00-baseline.sh</b><br/>step 0 evidence capture"]
        scan["<b>scripts/scan_evidence.py</b><br/>+ promote_trace.py<br/><i>the secret gate</i>"]
        fixtures["<b>configs/probes/</b><br/>system prompt + 6 fixtures"]
        evidence["<b>evidence/ · traces/ · decisions/</b><br/><i>the week's output</i>"]
        test_suite["<b>7-Layer Test Suite (94% coverage)</b><br/>unit · integration · functional · e2e<br/>journey · security · sanity"]
    end

    planlint[("planlint CLI")]
    sink[("eval-harness sink<br/><i>json_file artifacts</i>")]
    models["GitHub Models · Ollama (E:\\Ollama)<br/><i>real OpenAI-compatible HTTP</i>"]

    ab -->|"stdio · JSON-RPC"| server
    pg -.->|"same fixtures,<br/>pasted by hand"| fixtures
    probe_facade --> probe_cli
    probe_cli --> probe_runner
    probe_runner --> probe_screen
    probe_runner --> probe_client
    probe_client -->|"HTTP, http/https only"| models
    probe_pkg --> fixtures
    server -->|"subprocess<br/>read-only verbs"| planlint
    server -->|"file read"| sink
    baseline --> planlint
    probe_runner --> evidence
    baseline --> evidence
    ab -.->|"exported by hand"| evidence
    scan -->|"gate"| evidence
    test_suite --> server
    test_suite --> probe_pkg

    classDef store fill:#f5f5f5,stroke:#777
    class planlint,sink,models store
```

**Why the probe exists alongside the Playground.** The runbook calls the
verifier cell the week's most important signal and then measures it by reading
a GUI. The decomposed probe (`scripts/probe/`) sends the same system prompt and
fixture over HTTP to real local Ollama models (`E:\Ollama\ollama.exe`) or GitHub
Models and saves every transcript with a deterministic regex-based screen (`screen.py`),
so that one cell is reproducible.
It does not replace the Playground: only the Playground gives the local
resource profile for slots C and D.

### 7-Layer Test Architecture (Quality & Safety Gates)

The spike implements a rigorous enterprise 7-layer automated testing gate enforcing >=80% branch coverage (currently 94%):

1. **Unit Layer (`tests/unit/`, `mcp_server/tests/`)**: Pinned contract validation, modular probe config/screening unit checks, scoring branch coverage.
2. **Integration Layer (`tests/integration/`)**: Direct tool-function calls integrated with filesystem artifacts and the probe pipeline.
3. **Functional Layer (`tests/functional/`)**: `planlint` exit code mapping to tri-state verdicts (`PASS` 0, `FINDINGS` 1, `BLOCKED` 2/precondition).
4. **End-to-End Layer (`tests/e2e/`)**: Subprocess execution of `scan_evidence.py`, `promote_trace.py`, and `verifier_probe.py`.
5. **User Journey Layer (`tests/journey/`)**: Full developer evaluation lifecycle simulation from OpenSpec authoring to trace promotion.
6. **Security Layer (`tests/security/`)**: Fuzzing path traversal attacks, command-line flag injection, mutating verbs denial, and secret redaction.
7. **Sanity Layer (`tests/sanity/`)**: Enterprise environment health, PEP 561 markers, CLI entrypoints, and dependency validation.

---

## C3 — Components inside `mcp_server/`

```mermaid
graph TD
    sdk["MCP SDK<br/><i>FastMCP 1.x or MCPServer 2.x</i>"]
    server["<b>server.py</b><br/>transport only<br/><i>_load_server_class</i>"]

    planlint_mod["<b>planlint.py</b><br/><i>run_verb</i> — the single exec point<br/>lint_openspec · detect_dialect"]
    scoring["<b>scoring.py</b><br/>score_run<br/><i>true / false / null</i>"]

    guards["<b>guards.py</b><br/><b>POLICY — hard-coded</b><br/>verb allow list · flag deny list<br/>path containment · secret patterns<br/><i>redact_structure</i> — parsed payloads"]
    config["<b>config.py</b><br/><b>CONFIG — from env</b><br/>paths · timeouts · limits<br/>severity vocabulary<br/>findings byte + depth bounds"]
    verdicts["<b>verdicts.py</b><br/>PASS / FINDINGS / BLOCKED<br/><i>the only vocabulary</i>"]
    logs["<b>logging_setup.py</b><br/><i>stderr only</i>"]

    proc[("planlint<br/>subprocess")]
    artifact[("sink artifact<br/>JSON file")]
    payload["<b>findings payload</b><br/>bounded on UTF-8 bytes<br/><i>before</i> the parse<br/>then redacted structurally"]

    sdk --> server
    server --> planlint_mod
    server --> scoring
    planlint_mod --> guards
    planlint_mod --> config
    planlint_mod --> verdicts
    planlint_mod --> logs
    scoring --> guards
    scoring --> config
    scoring --> verdicts
    scoring --> logs
    planlint_mod --> proc
    scoring --> artifact
    proc -->|"stdout"| payload
    payload -->|"evidence only"| planlint_mod
    payload --> guards
    payload --> config

    classDef policy fill:#fff4e5,stroke:#c47f00,color:#000
    classDef cfg fill:#e8f2ff,stroke:#3a6ea5,color:#000
    class guards policy
    class config cfg
```

### The two rules this diagram encodes

**Policy and configuration are different things.** `config.py` reads the
environment — where planlint lives, how long to wait, which severity words this
build knows. `guards.py` reads nothing: the verb allow list, the flag deny list
and the credential patterns are hard-coded, because an allow list that can be
widened with an environment variable is not an allow list. Changing what the
tool may execute should require a code change, a review and a test.

**`server.py` holds no behaviour.** The tool modules import nothing from the
SDK, which is what lets the contract suite run with no dependency installed at
all. The cost of that separation, learned the hard way: nothing imported
`server.py` either, so it shipped against an SDK entry point that no longer
existed. CI now runs the transport as its own job.

### Verdict flow

```mermaid
flowchart LR
    call["tool call"] --> guard{"guards<br/>+ config"}
    guard -->|"refused · unusable path"| blocked["<b>BLOCKED</b><br/>blocked_reason"]
    guard -->|"allowed"| exec["subprocess"]
    exec -->|"timeout · missing binary · OSError"| blocked
    exec --> code{"exit code"}
    code -->|"0"| pass_["<b>PASS</b>"]
    code -->|"1"| findings["<b>FINDINGS</b>"]
    code -->|"2"| blocked
    code -->|"anything else"| blocked

    pass_ --> envelope["result envelope"]
    findings --> envelope
    blocked --> envelope

    subgraph evid["evidence path — runs after the verdict is already decided"]
        size{"stdout over<br/>findings_max_bytes?"}
        size -->|"yes"| trunc["bounded excerpt<br/><i>findings_truncated: true</i>"]
        size -->|"no"| parse{"parses as JSON?"}
        parse -->|"no"| perr["<i>findings_parse_error</i><br/>+ redacted excerpt"]
        parse -->|"yes"| red["<b>redact_structure</b><br/>keys and values<br/><i>past max_depth: marker</i>"]
    end

    exec -.->|"stdout"| size
    evid -.->|"<b>downgrades the evidence,<br/>never the verdict</b>"| envelope

    classDef b fill:#fff4e5,stroke:#c47f00,color:#000
    class blocked b
```

Every arrow into BLOCKED is a place an exception would otherwise have escaped.
That is the contract in one picture: **an exception is not a verdict**, and
there is no path from any failure to PASS.

The other half of the picture is an arrow that is not there. Nothing in the
evidence lane points back at the exit-code decision. A payload too large to hand
back, one that does not parse, or one a credential had to be redacted out of
arrives in the envelope degraded and says so — `findings_truncated`,
`findings_parse_error`, a depth marker — and leaves the verdict exactly where
the exit code put it. Exit 1 with an unreadable 30 MB payload is still FINDINGS.

---

## Week 2, if the verdict says proceed

Drawn to make the gap explicit, because the diagram is the argument.

```mermaid
graph LR
    hosted["Foundry Agent Service<br/><i>hosted agent</i>"]
    remote["Remote MCP endpoint<br/><i>Container Apps, internal ingress</i>"]
    vnet["Virtual network<br/><i>dedicated MCP subnet</i>"]
    stdio["<b>This week's stdio server</b>"]

    hosted -->|"needs"| remote
    remote --> vnet
    hosted -.->|"<b>cannot reach</b>"| stdio

    classDef gone fill:#eee,stroke:#999,stroke-dasharray:4 3
    class stdio gone
```

A hosted agent consumes **remote** MCP endpoints; Tool Catalog's local servers
are for use inside VS Code. None of the transport built this week carries over.
The **tool contract** does, and it is the part worth carrying — which is why
the contract, not the server, is what the test suite defends.

Cost and identity work for that step is enumerated in
`evidence/05-verdict.template.md` section 6, and is gated on the step 5 verdict.
